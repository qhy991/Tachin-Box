# 推箱子游戏打包说明

## 概述
本工具使用PyInstaller将推箱子游戏打包成独立的可执行文件，无需安装Python环境即可运行。

## 打包方式

### 方式一：使用Python脚本（推荐）
```bash
python build_executable.py
```

### 方式二：使用批处理文件
```bash
build.bat
```

## 打包过程

1. **环境检查**
   - 检查Python版本
   - 检查PyInstaller是否安装
   - 检查项目依赖是否完整

2. **依赖安装**
   - 自动安装缺失的PyInstaller
   - 自动安装requirements.txt中的依赖

3. **文件清理**
   - 删除之前的build目录
   - 删除之前的dist目录
   - 删除之前的.spec文件

4. **构建过程**
   - 使用PyInstaller打包
   - 包含所有必要的模块和数据文件
   - 生成单个可执行文件

## 输出文件

- **位置**: `dist/推箱子游戏.exe`
- **类型**: 独立的Windows可执行文件
- **大小**: 约100-200MB（包含所有依赖）

## 打包配置

### 包含的模块
- **GUI框架**: PyQt5
- **科学计算**: numpy, scipy, pandas
- **机器学习**: sklearn及其所有子模块
- **图像处理**: cv2, PIL, skimage
- **数据可视化**: matplotlib, seaborn
- **其他工具**: yaml, openpyxl, loguru等

### 包含的数据文件
- `interfaces/` - 界面文件
- `utils/` - 工具文件
- `data_processing/` - 数据处理文件
- `backends/` - 后端文件
- `config/` - 配置文件
- `extern/` - 外部文件
- `multiple_skins/` - 多皮肤文件
- `server/` - 服务器文件
- `with_nn/` - 神经网络文件
- `config.json` - 主配置文件

## 注意事项

1. **打包时间**: 首次打包可能需要5-15分钟，取决于系统性能
2. **文件大小**: 最终exe文件较大，这是正常的（包含Python解释器和所有依赖）
3. **杀毒软件**: 某些杀毒软件可能误报，这是正常现象
4. **运行环境**: 打包后的exe文件可以在没有Python的Windows系统上运行

## 故障排除

### 常见问题

1. **PyInstaller未安装**
   ```
   解决方案: 脚本会自动安装PyInstaller
   ```

2. **依赖缺失**
   ```
   解决方案: 脚本会自动安装requirements.txt中的依赖
   ```

3. **打包失败**
   ```
   解决方案: 检查错误信息，确保所有文件路径正确
   ```

4. **文件过大**
   ```
   解决方案: 这是正常现象，PyInstaller会包含完整的Python环境
   ```

## 高级配置

如果需要自定义打包配置，可以修改`build_executable.py`文件中的参数：

- `--onefile`: 生成单个exe文件
- `--windowed`: 隐藏控制台窗口
- `--add-data`: 添加数据文件
- `--hidden-import`: 添加隐藏导入的模块

## 联系支持

如果遇到问题，请检查：
1. Python版本是否为3.7+
2. 是否有足够的磁盘空间（至少500MB）
3. 是否有管理员权限
4. 防火墙和杀毒软件设置 